# This is a sample Python script.

# Press Shift+F10 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.


from leetcode import Solution


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    solutionObj = Solution()
    # assert solutionObj.removeDuplicateLetters("bcabc") == "abc", 'must be "abc"'
    # assert solutionObj.removeDuplicateLetters("cbacdcbc") == "acdb", 'must be "acdb"'

    # assert solutionObj.decodeAtIndex("leet2code3", 22) == "o", 'must be "o"'
    # assert solutionObj.decodeAtIndex("ha22", 5) == "h", 'must be "h"'
    # assert solutionObj.decodeAtIndex("a2345678999999999999999", 1) == "a", 'must be "a"'
    # assert solutionObj.decodeAtIndex("abc", 1) == "a", 'must be "abc"'
    # assert solutionObj.decodeAtIndex("a2b3c4d5e6f7g8h9", 3) == "b", 'must be "b"'
    # assert solutionObj.decodeAtIndex("a2b3c4d5e6f7g8h9", 9) == "b", 'must be "b"'

    # assert solutionObj.haveConflict(event1=["01:15", "02:00"], event2=["02:00", "03:00"]) is True, 'must be True'
    # assert solutionObj.haveConflict(event1=["01:00", "02:00"], event2=["01:20", "03:00"]) is True, 'must be True'
    # assert solutionObj.haveConflict(event1=["10:00", "11:00"], event2=["14:00", "15:00"]) is False, 'must be False'
    #
    # assert solutionObj.isMonotonic([1,2,2,3]) is True, 'must be True'
    # assert solutionObj.isMonotonic([6,5,4,4]) is True, 'must be True'
    # assert solutionObj.isMonotonic([1,3,2]) is False, 'must be False'

    # assert solutionObj.winnerOfGame("AAABABB") is True, 'must be true'
    # assert solutionObj.winnerOfGame("AA") is False, 'must be False'
    # assert solutionObj.winnerOfGame("ABBBBBBBAAA") is False, 'must be False'



# See PyCharm help at https://www.jetbrains.com/help/pycharm/
